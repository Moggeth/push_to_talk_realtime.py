# Agent Instructions

- Use Ruff for linting and formatting (`ruff check`, `ruff format`).
- Run tests with `pytest -vv` and keep outputs readable.
- Update `TESTING.md` when behavior or test coverage changes.
- Update `README.md` for user-visible changes.
